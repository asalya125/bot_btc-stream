# 🪙 Crypto Market Bot

Telegram-бот для отслеживания криптовалют с помощью web scraping.

## Файлы

```text
bot.py           — весь код бота
.env             — токен (секретный)
requirements.txt — зависимости
```

## Установка

```bash
pip install -r requirements.txt
```
```bash
pip3 install -r requirements.txt
```

## Настройка

Создайте файл `.env`:

```text
BOT_TOKEN=ваш_токен
```

Токен получить у [@BotFather](https://t.me/BotFather).

## Запуск

```bash
python bot.py
```

## Команды

- `/start` — запуск бота
- `/help` — справка
- `/stop` — остановка обновлений

## Как работает

1. Бот получает JSON данные с API Blockchain.info и DefiLlama
2. Обрабатывает данные (считает Net Flow стейблкоинов)
3. Формирует отчет и обновляет сообщение пользователю каждую минуту

## Особенности

- Использует **Long Polling** для получения команд
- Работает в многопоточном режиме (`threading`)
- Эффективно обновляет сообщения (`edit_message_text`), чтобы не засорять чат

